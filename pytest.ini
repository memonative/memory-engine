[pytest]
testpaths = tests
pythonpath = src client/src
markers =
    perf: slow performance tests that require a live Postgres; skip in default CI with -m 'not perf'
    dbintegration: tests that require a live Postgres + pgvector stack; skip in DB-less CI with -m 'not dbintegration'
